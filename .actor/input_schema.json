{
	"$schema": "https://apify.com/schemas/v1/input.ide.json",
	"title": "TripAdvisor Reviews Scraper Input",
	"type": "object",
	"schemaVersion": 1,
	"properties": {
		"startUrls": {
			"title": "Place URLs",
			"type": "array",
			"description": "TripAdvisor place URLs to scrape reviews from (hotels, restaurants, attractions)",
			"editor": "requestListSources",
			"prefill": [
				{
					"url": "https://www.tripadvisor.com/Hotel_Review-g190327-d264936-Reviews-1926_Hotel_Spa-Sliema_Island_of_Malta.html"
				}
			]
		},
		"startDate": {
			"title": "Start Date",
			"type": "string",
			"description": "Only include reviews published on or after this date (YYYY-MM-DD). Leave empty for all reviews.",
			"default": "",
			"editor": "datepicker",
			"dateType": "absolute"
		},
		"maxReviewsPerPlace": {
			"title": "Max Reviews per Place",
			"type": "integer",
			"description": "Maximum number of reviews to scrape per place (0 = unlimited)",
			"default": 0,
			"minimum": 0,
			"unit": "reviews"
		},
		"proxyConfiguration": {
			"title": "Proxy Configuration",
			"type": "object",
			"description": "Proxy settings for anti-bot protection. Recommended for high-volume runs.",
			"editor": "proxy",
			"default": {
				"useApifyProxy": false
			}
		},
		"captchaWaitSeconds": {
			"title": "Captcha wait (seconds)",
			"type": "integer",
			"description": "If captcha appears before scraping, set this to give time to solve it (e.g. 15–30). The actor will wait this many seconds with a message to solve captcha if visible. Use when captcha always appears but is not auto-detected.",
			"default": 0,
			"minimum": 0,
			"maximum": 120,
			"unit": "seconds"
		},
		"tryAutoSlide": {
			"title": "Try auto-slide captcha",
			"type": "boolean",
			"description": "When true, attempts to automatically solve the slide captcha by simulating a drag. Experimental — may not work with PerimeterX. Falls back to 2Captcha or manual solve if it fails.",
			"default": false
		},
		"debugCaptcha": {
			"title": "Debug captcha",
			"type": "boolean",
			"description": "When true, logs captcha-related iframes (captcha-delivery.com only). Run with captcha visible to debug.",
			"default": false
		}
	},
	"required": ["startUrls"]
}
